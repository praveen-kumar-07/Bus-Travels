services:
  - type: web
    name: django-travels
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn travels.wsgi
    envVars:
      - key: DEBUG
        value: False
